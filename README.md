# Accenture
Setup
- Node.js v16
- Navigate to the `frontend` directory.
    ```bash
    cd frontend
    ```
- Install the required packages using `yarn`.
    ```bash
    yarn
    ```
- Start the application.
    ```bash
    yarn start
    ```
